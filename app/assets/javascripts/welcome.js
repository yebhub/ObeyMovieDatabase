// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
// You can use CoffeeScript in this file: http://coffeescript.org/

//try using either fetch or asynchronous ajax HTTP request

$(document).ready(function(){
    console.log("test")



//     //create a method that makes a call to the search term we queried and then use it in the ajax form we made. 

   /* $('.search_button').on("click", function(e){
        e.preventDefault();
            console.log($(this))*/
/*
      $.ajax({
      url: $(".search_button").attr("href"),
      method: $(".search_form").attr("method"),
      data: $(".search_box").serialize(),
      success: function(data){
      console.log(data)
       // var signup_form = $(response).find("#sign_up_form");
         //  $("#opening_forms").html(signup_form[0])
      }
    })
  });*/

});
